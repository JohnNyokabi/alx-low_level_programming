The tasks contains code that:
	Prints whether a number is positive or negative.
	Comments the part of the code that is causing the output to go into an infinite loop.
	Prints the largest of the 3 integers.
	Converts a date to the day of year and determines how many days are left in the year, taking leap year into consideration.
