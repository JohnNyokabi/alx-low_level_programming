#include "main.h"

/**
 * _abs - function that computes the absolute value
 * @n: number being tested
 *
 * Return: Always 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	return (0);
}
